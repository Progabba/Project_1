Index: src/services.py
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>import os\r\nfrom typing import Dict, List, Any\r\n\r\nimport pandas as pd\r\n\r\nfrom src.utils import load_excel_to_dataframe\r\n\r\ncurrent_dir = os.getcwd()\r\nparrent_dir = os.path.dirname(current_dir)\r\ndata_dir = os.path.join(parrent_dir, 'data')\r\nfile_path = os.path.join(data_dir, 'operations.xls')\r\n\r\n\r\n\r\ndef search_phone_numbers(data: pd.DataFrame) -> List[Dict[str, Any]]:\r\n    \"\"\"Поиск транзакций с мобильными номерами в описании.\"\"\"\r\n    return data[data['Описание'].str.contains(r'\\+\\d{1,2}\\s\\d{3}\\s\\d{2,3}-\\d{2}-\\d{2}', regex=True)].to_dict(orient='records')\r\n\r\nif __name__ == '__main__':\r\n    data = load_excel_to_dataframe(file_path)\r\n    print(search_phone_numbers(data))
===================================================================
diff --git a/src/services.py b/src/services.py
--- a/src/services.py	
+++ b/src/services.py	
@@ -1,3 +1,4 @@
+import logging
 import os
 from typing import Dict, List, Any
 
@@ -10,11 +11,19 @@
 data_dir = os.path.join(parrent_dir, 'data')
 file_path = os.path.join(data_dir, 'operations.xls')
 
+logger = logging.getLogger("services")
+logger.setLevel(logging.INFO)
+file_handler = logging.FileHandler("../logs/services.log", mode="w")
+file_formatter = logging.Formatter("%(asctime)s - %(name)s - %(levelname)s - %(message)s")
+file_handler.setFormatter(file_formatter)
+logger.addHandler(file_handler)
 
 
 def search_phone_numbers(data: pd.DataFrame) -> List[Dict[str, Any]]:
     """Поиск транзакций с мобильными номерами в описании."""
-    return data[data['Описание'].str.contains(r'\+\d{1,2}\s\d{3}\s\d{2,3}-\d{2}-\d{2}', regex=True)].to_dict(orient='records')
+    result = data[data['Описание'].str.contains(r'\+\d{1,2}\s\d{3}\s\d{2,3}-\d{2}-\d{2}', regex=True)].to_dict(orient='records')
+    logger.info(f"Количество операций с моб. телефонами равно: {len(result)} ")
+    return result
 
 if __name__ == '__main__':
     data = load_excel_to_dataframe(file_path)
